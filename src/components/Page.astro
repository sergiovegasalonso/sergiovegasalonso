<main
  class="h-full w-full overflow-x-hidden overflow-y-scroll border border-2 border-(--border-color) bg-(--foreground-color) p-8 shadow-(--shadow) lg:w-4xl"
>
  <slot />
</main>

<style>
  main {
    scrollbar-color: var(--scrollbar-style);
    scrollbar-width: auto;
  }
</style>
